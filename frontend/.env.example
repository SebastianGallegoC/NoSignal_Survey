VITE_API_URL=""
# URL base del backend (ej. http://127.0.0.1:8000). Vacío = mismo origen (proxy en despliegue).

# Opcional: URL completa del ingest de depuración del agente (solo si tenés el recolector corriendo).
# Sin definir, no se hace ningún POST y no aparece ERR_CONNECTION_REFUSED en consola.
# VITE_AGENT_INGEST_URL=http://127.0.0.1:7372/ingest/<tu-id>
